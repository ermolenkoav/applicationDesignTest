definitions:
  model.Order:
    properties:
      email:
        type: string
      from:
        type: string
      hotel_id:
        type: string
      room_id:
        type: string
      to:
        type: string
    type: object
info:
  contact: {}
paths:
  /api/v1/orders:
    post:
      description: Create a hotel room booking order
      parameters:
      - description: hotel_id, room_id, email, from, to
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/model.Order'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/model.Order'
        "400":
          description: Bad Request
          schema:
            type: string
        "422":
          description: Unprocessable Entity
          schema:
            type: string
      summary: Booking
swagger: "2.0"
